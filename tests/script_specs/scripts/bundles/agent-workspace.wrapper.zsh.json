{
  "smoke": [
    {
      "name": "load",
      "command": ["zsh", "-f", "scripts/bundles/agent-workspace.wrapper.zsh"],
      "timeout_sec": 10,
      "expect": {
        "exit_codes": [0]
      }
    },
    {
      "name": "sources_array",
      "command": [
        "zsh",
        "-f",
        "-c",
        "source scripts/bundles/agent-workspace.wrapper.zsh; [[ ${#sources[@]} -eq 5 ]] && [[ ${sources[1]} == _features/agent-workspace/alias.zsh ]] && [[ ${sources[5]} == _features/agent-workspace/workspace-launcher.zsh ]] && print -r -- OK"
      ],
      "timeout_sec": 10,
      "expect": {
        "exit_codes": [0],
        "stdout_regex": "(?m)^OK$"
      }
    }
  ]
}
